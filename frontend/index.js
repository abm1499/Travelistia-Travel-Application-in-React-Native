/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Stack from './source/navigations/stack/Stack';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Stack);
